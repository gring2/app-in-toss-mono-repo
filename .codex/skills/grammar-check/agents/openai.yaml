interface:
  display_name: "Grammar Check"
  short_description: "Identify grammar, logical, and flow errors in text and suggest…"
  default_prompt: "Use $grammar-check to identify grammar, logical, and flow errors in text and suggest targeted fixes without rewriting the entire text."
