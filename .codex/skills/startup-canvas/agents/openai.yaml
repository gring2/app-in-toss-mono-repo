interface:
  display_name: "Startup Canvas"
  short_description: "Generate a Startup Canvas combining Product Strategy (9 sections)…"
  default_prompt: "Use $startup-canvas to generate a Startup Canvas combining Product Strategy (9 sections) and Business Model (costs + revenue) for a new product."
